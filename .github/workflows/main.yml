name: Publish to NPM and Docker

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v1.0.0

      - name: Set up Node.js
        uses: actions/setup-node@v1.0.0
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Publish to NPM
        run: npm publish

      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t your-docker-username/hostname-web-app .
          docker push your-docker-username/hostname-web-app
